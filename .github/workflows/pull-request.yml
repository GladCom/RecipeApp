name: Pull request build

on:
  pull_request:
    branches:
      - dev
      - master

jobs:
  pull_request_build:
    name: Run build
    runs-on: ubuntu-latest

    steps:
      - name: Run StyleCop check
        uses: Dezodemius/RecipeApp/.github/workflows/build.yml@dev
        with:
          solution-path: ./RecipeApp/RecipeApp.sln